# Firebase credentials (included in releases for remote logging)
# firebase-credentials.json

# Python cache
__pycache__/
*.pyc

# Local log files
*.log
session.log

# Firmware directories (downloaded binaries)
production_firmware/
testing_firmware/

# Temporary files
temp_efuse_*.bin

# Release ZIP files (created during release process)
*.zip
dino-production-flasher-v*.zip
